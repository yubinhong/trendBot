services:
  short-trend-bot:
    build: .
    container_name: short-crypto-trend-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONIOENCODING=utf-8
    depends_on:
      short-trend-mysql:
        condition: service_healthy
    networks:
      - short-trend-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"



  short-trend-mysql:
    image: mysql:8.0
    container_name: short-trend-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DB}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
    networks:
      - short-trend-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

volumes:
  mysql_data:

networks:
  short-trend-network:
    driver: bridge